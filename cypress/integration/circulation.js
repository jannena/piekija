
before(() => {
    // TODO: something
    // TODO: Log in as staff user
});

describe("when staff user is logged in", () => {
    it("user and item can be searched AND item can be loaned to user AND item can be returned", () => { });
    it("holds can be received AND item can be received for user AND it can be marked as pick-up AND loaned to user AND returned AND same again with another hold placer", () => { });
});