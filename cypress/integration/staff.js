
before(() => {
    // TODO: Do something
});

describe("", () => {
    it("location can be created AND edited AND removed", () => { });
    it("loantype can be created AND edited AND removed", () => { });
    it("front page news can be created AND edited AND removed", () => { });
    it("users can be created AND edited AND removed", () => { });
});